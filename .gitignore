node_modules
dist
.DS_Store
*.log
.vite

# Local Claude Code dev config
.claude/

# Local test images (examples/istockphoto-*.jpg is stock photography
# with restrictive redistribution terms — kept out of the repo)
examples/

# Source screen recordings live in demo/ but only the rendered GIF is tracked
demo/*.mp4
demo/*.mov
demo/*.webm

# Animated crowd GLBs (~430 MB total; two files exceed GitHub's 100 MB
# per-file limit). Distributed as GitHub release assets instead — the app
# streams them from the release URL when no local copy is served.
public/crowd-models/

# Local 3D-model working files and prototypes, not part of the app
3d humans/
crowd_density_3d_visualizer.html
